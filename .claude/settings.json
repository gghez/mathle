{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | tr -d '\\r' | { read -r f; case \"$f\" in *.ts|*.js|*.json|*.md|*.css|*.html) npx prettier --write \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Prettier"
          }
        ]
      }
    ]
  }
}
